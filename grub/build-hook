#!/bin/bash

set -e

unset CC CFLAGS LD
export LDFLAGS="-static"

./configure           \
	--target=$TARGET  \
	--host=$TARGET    \
	--prefix=/usr     \
	--sysconfdir=/etc \
	--disable-efiemu  \
	--disable-werror
make -j${JOBS}

# Installing
make DESTDIR=$SYSROOT install
