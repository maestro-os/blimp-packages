#!/bin/bash

set -e
cd *

mkdir build
cd build

../configure \
	--prefix=/usr \
	--build="$BUILD" \
	--host="$HOST" \
	--target="$TARGET" \
	--enable-shared \
	--disable-werror \
	--enable-64-bit-bfd
make -j${JOBS}

make DESTDIR=$SYSROOT install -j1
